# 🚀 快速開始指南 (Quick Start Guide)

## 📦 專案概覽

**Ollama 基準測試與自動化管道** 是一個使用者友善的系統，用於：
- 自動處理圖像並生成描述
- 使用 Ollama 或 Gemini 進行物件偵測
- 提供審核隊列與批准工作流程
- 支援繁體中文優先介面

---

## ⚡ 3 分鐘快速設定

### 前置需求
- Python 3.8+ 已安裝
- （選用）Ollama 已安裝並執行

### 步驟 1: 安裝依賴 (30 秒)
```bash
cd backend
pip install -r requirements.txt
```

### 步驟 2: 初始化系統 (1 分鐘)
```bash
# 初始化資料庫
python init_db.py

# 驗證配置
python validate_config.py
```

### 步驟 3: 啟動 UI (1 分鐘)
```bash
# 選項 A: 網頁版管道 UI
cd ../ollama-benchmark-pipeline-ui
npm install
npm run dev

# 選項 B: 整合桌面應用程式 (Flutter)
cd ../ollama_benchmark_flutter
flutter pub get
flutter run
```

**完成！** 🎉 

---

## 🎯 使用流程

### 情境 1: 處理第一張圖像

1. **放置圖像**
   - 將圖像檔案放入 `backend/input/` 資料夾
   - 支援格式：JPG, PNG, WebP

2. **自動處理**
   - 系統自動偵測新檔案
   - 生成描述與元數據
   - 進入審核隊列

3. **審核與批准**
   - 在 UI 中查看「待審核隊列」
   - 點擊項目查看詳細資訊
   - 選擇「批准」或「拒絕」

4. **查看結果**
   - 批准的項目移至 `backend/output/`
   - 包含：描述檔案、元數據、縮圖

---

## ⚙️ 配置調整

### 啟用 Ollama
編輯 `backend/config/jade_config.yaml`：
```yaml
ollama:
  enabled: true  # 改為 true
  url: "http://localhost:11434"
  model: "llama3.2-vision"
```

### 啟用 Gemini 備援
```yaml
gemini:
  enabled: true
  api_key: "YOUR_GEMINI_API_KEY"
```

### 調整自動批准閾值
```yaml
processing:
  auto_approve_confidence: 0.85  # 0.0-1.0
```

---

## 🧪 驗證安裝

### 檢查資料庫
```bash
sqlite3 backend/db/pipeline.db ".tables"
```
**預期輸出：** `pipeline_items`, `approval_history`, `system_logs`, `config_snapshots`

### 檢查目錄結構
```bash
dir backend\input backend\output backend\templates
```
**預期輸出：** 所有目錄存在

### 測試配置
```bash
cd backend
python validate_config.py
```
**預期輸出：** ✅ 配置驗證通過

---

## 🎨 UI 功能導覽

### 儀表板 (Dashboard)
- 查看系統統計資料
- 監控處理狀態
- 檢視平均處理時間

### 待審核隊列 (Pending Queue)
- 查看所有待審核項目
- 快速批准/拒絕
- 批量操作

### 已處理記錄 (Processed History)
- 查看所有歷史記錄
- 篩選與搜尋
- 匯出報告

### 系統設定 (Settings)
- 調整 Ollama/Gemini 設定
- 配置路徑與閾值
- 查看系統狀態

---

## 🔧 疑難排解

### 問題：資料庫初始化失敗
**症狀：** `init_db.py` 執行錯誤  
**解決方案：**
1. 確認 `backend/db/` 目錄存在
2. 檢查檔案權限
3. 刪除舊的 `pipeline.db` 後重試

### 問題：Ollama 連接失敗
**症狀：** UI 顯示「Ollama 離線」  
**解決方案：**
1. 確認 Ollama 已啟動：`ollama serve`
2. 檢查 `jade_config.yaml` 中的 URL
3. 測試連接：`curl http://localhost:11434`

### 問題：UI 無法啟動
**症狀：** `npm run dev` 或 `flutter run` 失敗  
**解決方案：**
- **Web (React):**
  1. 刪除 `node_modules/` 並重新安裝
  2. 檢查 Node.js 版本 (需要 16+)
- **Desktop (Flutter):**
  1. 確認 Flutter SDK 已安裝且在 PATH 中
  2. 執行 `flutter doctor` 檢查環境
  3. 執行 `flutter pub get` 更新依賴

### 問題：圖像未被處理
**症狀：** 放入 `input/` 的圖像沒有反應  
**解決方案：**
1. 確認檔案格式正確 (JPG/PNG/WebP)
2. 檢查檔案大小 (預設限制 50MB)
3. 查看 `backend/logs/` 錯誤訊息

---

## 📚 進階主題

### 啟用守護程序模式
```yaml
processing:
  enable_daemon: true
  watch_interval_seconds: 5
```

### 多語言支援
```yaml
output:
  description_languages: ["zh-TW", "en"]
```

### 自訂模板
編輯 `backend/templates/description.zh-TW.md` 以自訂輸出格式

---

## 🆘 取得協助

- **文件：** 查看 `backend/README.md`
- **測試計畫：** 參考 `backend/tests/PHASE_0_TEST_PLAN.md`
- **設計規格：** 閱讀 `Design_Specification.md`
- **問題回報：** 建立 GitHub Issue

---

## 🎓 下一步學習

1. **Phase 1：** 了解單次執行管道
2. **Phase 2：** 探索守護程序與審核隊列
3. **Phase 3：** 學習錯誤處理與備援機制
4. **Phase 4：** 自訂 UI 與本地化
5. **Phase 5：** 部署到生產環境

---

**祝您使用愉快！** 🚀
