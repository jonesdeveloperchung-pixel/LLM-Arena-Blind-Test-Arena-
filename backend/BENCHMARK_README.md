# 🧪 LLM 基準測試系統

## 📋 功能概覽

實作 Requirement_Specification.md 中定義的 5 大能力評測：

1. **推理能力** (Reasoning) - 邏輯推理、多步驟問題
2. **編碼能力** (Coding) - 程式生成、除錯、複雜度分析
3. **視覺語言** (Vision-Language) - 圖像理解、多模態查詢
4. **通用語言** (General Language) - 流暢度、創意、對話
5. **嵌入能力** (Embedding) - 語義相似度、檢索

---

## 🚀 快速開始

### 測試單一模型
```bash
# 測試預設模型
python benchmark.py

# 測試指定模型
python benchmark.py llama3.2-vision:latest
```

### 批次測試推薦模型
```bash
python benchmark_batch.py
```

推薦模型配置：
- 推理：`deepseek-r1:32b`
- 編碼：`qwen2.5-coder:latest`
- 視覺：`llama3.2-vision:latest`
- 通用：`llama3.2:latest`
- 嵌入：`nomic-embed-text:v1.5`

---

## 📊 評分系統

### Gemini-as-Judge
- 使用 Gemini 2.0 Flash 作為評審
- 評分範圍：1-5 星
- 提供詳細評審理由
- 各項指標細分評分

### 評分標準
| 分數 | 等級 | 說明 |
|------|------|------|
| 5 | 優秀 | 完美回應，超越預期 |
| 4 | 良好 | 正確且完整 |
| 3 | 中等 | 基本正確但有改進空間 |
| 2 | 不佳 | 部分錯誤或不完整 |
| 1 | 失敗 | 嚴重錯誤或無法回應 |

---

## 📈 輸出格式

### 單一測試結果
```json
{
  "model": "llama3.2:latest",
  "category": "reasoning",
  "score": 4,
  "reasoning": "推理步驟清晰...",
  "breakdown": {
    "accuracy": 4,
    "step_clarity": 5,
    "consistency": 4
  }
}
```

### 批次測試報告
```json
{
  "llama3.2:latest": [...],
  "deepseek-r1:32b": [...],
  ...
}
```

---

## 🎯 測試案例

### 1. 推理能力
**測試：** 邏輯推理問題  
**提示：** "如果 A > B 且 B > C，那麼 A 和 C 的關係是什麼？"  
**評估：** 準確度、步驟清晰度、一致性

### 2. 編碼能力
**測試：** 程式生成  
**提示：** "寫一個計算斐波那契數列的函式"  
**評估：** 功能正確性、效率、可讀性

### 3. 視覺語言
**測試：** 圖像描述  
**提示：** "描述這張圖像的內容"  
**評估：** 準確度、深度、對齊度

### 4. 通用語言
**測試：** 創意寫作  
**提示：** "寫一個關於 AI 的短文"  
**評估：** 流暢度、連貫性、創意

### 5. 嵌入能力
**測試：** 語義相似度  
**提示：** "計算兩個句子的相似度"  
**評估：** 相似度準確性、聚類、檢索

---

## 🔧 配置

### 啟用 Gemini 評審
編輯 `config/jade_config.yaml`：
```yaml
gemini:
  enabled: true
  api_key: "YOUR_GEMINI_API_KEY"
  model: "gemini-2.0-flash-exp"
```

### 自訂測試案例
編輯 `benchmark.py` 中的 `CATEGORIES` 字典。

---

## 📊 與 UI 整合

### ollama-benchmark-suite UI
1. 啟動後端 API（待實作）
2. UI 呼叫 `benchmark.py`
3. 即時顯示評分與雷達圖

### 資料流程
```
UI → benchmark.py → Ollama API → Gemini Judge → UI
```

---

## 🎓 進階功能

### 自訂評審標準
```python
def custom_judge(output, category):
    # 實作自訂評分邏輯
    return {'score': 5, 'reasoning': '...'}
```

### 匯出報告
```bash
# 生成 Markdown 報告
python benchmark.py llama3.2:latest --format markdown

# 生成 CSV 報告
python benchmark.py llama3.2:latest --format csv
```

---

## 📚 參考資料

- [Requirement_Specification.md](../Requirement_Specification.md)
- [LLM Capability Comparison.md](../LLM Capability Comparison.md)
- [Overlay-Benchmarking_Spec_LLM_Performance.md](../Overlay-Benchmarking_Spec_LLM_Performance.md)

---

**完整實作 Requirement Specification！** ✅
